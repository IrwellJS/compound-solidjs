export enum Fit {
    Justify = 'justify',
    Inline = 'inline',
}

export enum Size {
    Small = 'small',
    Medium = 'medium',
    Large = 'large',
}

export enum Variant {
    Primary = 'primary',
    Secondary = 'secondary',
}
